# Phase D.3 — production application dependencies
# Install engine scientific deps from STEEL_ENGINE_ROOT/requirements.txt separately
# when the engine is not yet packaged inside current_model/.

Flask>=3.0,<4
python-dotenv>=1.0,<2
PyYAML>=6.0,<7
gunicorn>=22.0,<24
