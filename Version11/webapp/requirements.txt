# Phase W.2 web layer
flask>=3.0.0
werkzeug>=3.0.0
gunicorn>=21.0.0

# Engine (T1 / VB.1 / DXF) dependencies live in Version10/requirements.txt.
# Install that file as well before running a live estimation.
